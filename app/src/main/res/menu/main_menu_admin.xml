<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    tools:ignore="HardcodedText">

    <group android:checkableBehavior="single">
        <item android:id="@+id/home"
            android:icon="@drawable/home_icon"
            android:title="Home"/>

        <item android:id="@+id/payments"
            android:icon="@drawable/payments_icon"
            android:title="Report"/>

        <item android:id="@+id/reg_mech"
            android:icon="@drawable/sell__icon"
            android:title="Register Mech" />

        <item android:id="@+id/logout_admin"
            android:icon="@drawable/access_history"
            android:title="Logout"/>
    </group>

    <item android:title="Profile">
        <menu>
            <group android:checkableBehavior="single">
                <item android:id="@+id/about"
                    android:icon="@drawable/adjust_about"
                    android:title="Mechanics"/>
            </group>
        </menu>
    </item>

    <item android:title="Communication">
        <menu>
            <group android:checkableBehavior="single">
                <item android:id="@+id/support"
                    android:icon="@drawable/call_support"
                    android:title="News"/>

                <item android:id="@+id/manage"
                    android:icon="@drawable/call_support"
                    android:title="Manage News"/>
            </group>
        </menu>
    </item>

</menu>